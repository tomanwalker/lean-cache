{
	"name": "lean-cache",
	"version": "0.1.1",
	"description": "Lean and customizable in-memory cache",
	"main": "index.js",
	"scripts": {
		"test": "mocha --exit test/*.js",
		"test-verbose": "mocha --exit --reporter json test/*.js &> ./test/report.json",
		"coverage": "istanbul cover --config=istanbul.yml --report html --report lcov node_modules/mocha/bin/_mocha -- --exit test/*.js"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/AlexShkunov/lean-cache"
	},
	"keywords": [
		"microservices",
		"cache",
		"fifo",
		"lru",
		"memory",
		"ttl",
		"keyvalue"
	],
	"author": "tomanwalker",
	"license": "MIT",
	"devDependencies": {
		"mocha": "*",
		"istanbul": "*",
		"chai": "*",
		"proxyquire": "*"
	}
}
